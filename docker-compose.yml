version: '3.8'

services:
  garden-tracker:
    build: .
    container_name: garden-tracker
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_PATH=/app/data/garden.db
      - PORT=8000
      - PERENUAL_API_KEY=sk-iLPf696e73541926614416
    restart: unless-stopped
